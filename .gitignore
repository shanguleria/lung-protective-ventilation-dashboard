.venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.DS_Store

# Site-specific config (each site copies config.example.json -> config.json)
config.json

# Internal working logs (not part of the shared template)
.claude/

# Scorecard tile artwork (local; the scorecard falls back to inline SVG icons without them)
references/images/

# Generated outputs and intermediate data — kept out of git
output/
*.parquet
*.csv

# Editor / OS cruft
.vscode/
.idea/
*.swp
